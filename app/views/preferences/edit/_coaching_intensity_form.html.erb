<%= form_for @preference, :html => { :class => "form-horizontal" } do |f| %>
	<fieldset>
		<%= render :partial => "preferences/edit/coaching_intensity", :locals => {:f => f} %>
		<div class="form-actions">
			<button type="submit" class="btn btn-primary">Update</button>
		</div>
	</fieldset>
<% end %>