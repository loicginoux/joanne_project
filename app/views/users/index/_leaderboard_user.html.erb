<div>
	<%= link_to user_path(:username=> user.username), :class => "user" do %>
		<div class="rank pull-left well">
			<span>
				<% if defined? current_user_position %>
					<%= current_user_position %>
				<% else %>
					<%= ((leaderboard_users.current_page * users_per_page) - (users_per_page - 1)) + index %>
				<% end %>
			</span>
		</div>

		<div class="image pull-left">
			<%=  image_tag user.picture.url(:small) %>
			<span class="username"><%= user.username%></span>
		</div>
		<% if leaderboard == "total_leaderboard" %>
			<div class="winner-ribbons pull-left">
				<% user.leaderboard_prices.last(3).each do |price| %>
					<%= image_tag "month_winner_ribbon.svg", :title => price.name, :style => "width:40px", :class=> "winner-ribbon" %>
				<% end %>
			</div>
		<% end %>

		<div class="pull-right points">
			<p><%= user[leaderboard+"_points"]%> points</p>
			<%= render :partial => "users/index/follow_button", :locals => {:user => user}%>
		</div>
	<% end %>
</div>