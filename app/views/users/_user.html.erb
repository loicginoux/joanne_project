<div class="span4">
	<div class="image pull-left">
		<%= link_to user_path(:username=> user.username) do %>
			<%=  image_tag user.picture.url(:small) %>
			<%= user.username%>
		<% end %>
	</div>
	<% if current_user.isFollowing(user).empty? %>
		<%= link_to "Follow", friendships_path(:followee => user.id), :method => :post, :class => "btn btn-primary pull-right"  %>
	<% else %>
		<%= link_to "Unfollow", current_user.isFollowing(user)[0], :method => :delete, :class => "btn btn-primary pull-right"  %> 				
	<% end %> 
	  	
</div>