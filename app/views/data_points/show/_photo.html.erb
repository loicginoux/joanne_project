<% cache [data_point, size, "v2"] do %>


	<div class="img_wrapper <%= size %>">
		<% if data_point.has_award? %>
			<div class="ribbon-wrapper right">
				<div class="ribbon">
					<span><%= data_point.award %></span>
				</div>
			</div>
		<% end %>
		<%= image_tag(data_point.pic().url(size)) %>
	</div>

<% end %>