foodrubix.users.index = () ->
	$(".winner-ribbon").tooltip()
	# $(".total_leaderboard_users .user").each((i,el) ->
	# 	el = $(el);
	# 	w = el.find('.winner-ribbons')
	# 	if w.length
	# 		r = el.find(".rank")
	# 		i = el.find(".image")
	# 		p = el.find(".points")
	# 		width = el.width() - i.width() - p.width() - r.width();
	# 		prices = w.find("winner-ribbon")
	# 		prices.css("width", width/prices.length)
	# )


